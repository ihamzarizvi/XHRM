[zkteco]
ip = 192.168.11.201
port = 4370
password = 1111

[xhrm]
# XHRM server URL (no trailing slash)
url = https://mimar.xsofty.com
# API key for automated attendance import (must match server-side key)
api_key = xhrm-zkteco-sync-2024-secret-key

[sync]
# Sync interval in minutes (0 = manual only, 2 = realtime, 5/15/30/60 = periodic)
interval = 5
# Your timezone
timezone = Asia/Karachi
timezone_offset = 5.0

[backup]
# Local SQLite database path (relative to this directory)
db_path = ./attendance_backup.db
# CSV export directory
csv_export_path = ./exports
